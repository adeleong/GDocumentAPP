@model DEPENDENCIA

@{
    ViewBag.Title = "Details";
}
<br /><br />
<h2>Dependencia Detalle</h2>

<div class="panel panel-default">
    <div class="panel-heading">
        <h3 class="panel-title">Dependencia</h3>
    </div>
    <div class="panel-body">

        <div>
            <dl class="dl-horizontal">
                <dt>
                    @Html.DisplayNameFor(model => model.DEPENDENCIA_NOMBRE)
                </dt>

                <dd>
                    @Html.DisplayFor(model => model.DEPENDENCIA_NOMBRE)
                </dd>

            </dl>
        </div>
    </div>

    <div class="panel-footer">

        <div class="input-group col-md-push-1">
            @Html.ActionLink("Editar", "Edit", new { id = Model.DEPENDENCIA_ID }, new { @class = "btn btn-warning" })
            <span> </span>
            @Html.ActionLink("Volver al Listado", "Index", null, new { @class = "btn btn-info" })
        </div>
    </div>
</div>
