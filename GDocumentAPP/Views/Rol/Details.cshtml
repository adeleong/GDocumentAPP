@model GDocumentAPP.ROL

@{
    ViewBag.Title = "Details";
}

<br /><br />
<h2>Rol Detalle</h2>

<div class="panel panel-default">
    <div class="panel-heading">
        <h3 class="panel-title">Rol</h3>
    </div>
    <div class="panel-body">
        <div>
            <dl class="dl-horizontal">
                <dt>
                    @Html.DisplayNameFor(model => model.DESCRIPCION)
                </dt>

                <dd>
                    @Html.DisplayFor(model => model.DESCRIPCION)
                </dd>

            </dl>
        </div>
    </div>

    <div class="panel-footer">

        <div class="input-group col-md-push-1">
            @Html.ActionLink("Editar", "Edit", new { id = Model.ROL_ID }, new { @class = "btn btn-warning" })
            <span> </span>
            @Html.ActionLink("Volver al Listado", "Index", null, new { @class = "btn btn-info" })
        </div>
    </div>
</div>
